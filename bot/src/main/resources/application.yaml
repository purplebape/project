app:
  test: hello-bot
  bot:
    name: "projectink_bot"
    token: ${BOT_TOKEN}
  scrapper:
    url: "localhost"

springdoc:
  swagger-ui:
    path: "swagger-ui"

management:
  server:
    port: 8080